# 「计时器」组件规划

> 多预算计时器：给每件事设一个时间额度（写作业 1h、玩游戏 1h……），
> 做哪件事就点开哪一个，随时暂停；跨天（或按周 / 手动）自动清零。
> 纯自律工具——不锁屏、不拦截，你不想管它也可以，想删掉也可以。
>
> **形态**：不是独立桌面小组件（太多会摆不下）。入口是**时钟组件番茄钟界面里
> 的一个小按钮**，点击打开一个**白色普通桌面窗口**，里面列着今天的所有计时器。
>
> **视觉硬要求**：必须好看。对齐 DESIGN.md 的五套风格体系与 macOS 设计语言——
> SF 系字体、大圆角、充足留白、克制的动效（150–250ms ease-out）、
> 语义化状态色；计时器窗口虽是白底应用风，质感要向 macOS 系统应用看齐
> （参考提醒事项 / 时钟.app 的列表与表单）。

## 1. 需求理解

一句话：**「时间预算本」**。每个计时器 = 一个名字 + 一段预算时间。

典型场景：

- 开始学习 → 在计时器窗口里点开「学习」开始走字，中间去倒水 → 暂停，回来 → 继续。
- 「玩游戏」预算 1 小时，用完弹一个轻提醒，界面变红——看得到的额度，比意志力好使。
- 第二天打开，昨天的全部清零，重新开始；也可设成按周清，或只手动清。
- 开番茄钟专注时，可以顺手指定「这个番茄算在哪个计时器头上」。

## 2. 形态与窗口架构

```
桌面时钟组件（clock）
   └─ 番茄钟界面角落一个小按钮（秒表图标，有计时器在走时带状态点）
        │ 点击
        ▼
   白色普通窗口（timer.html，新 BrowserWindow，约 320×420 可调）
   · 非桌面贴靠小组件：正常窗口，可拖动/关闭，像个小应用
   · 列出今天的计时器列表
   · 再点入口按钮 = 显示/隐藏切换（toggle）
```

实现要点（对照现有代码）：

- **主进程**：`main.js` 仿照控制中心 `managerWin`（main.js:217）的模式，维护
  `timerWin` 单例：不存在就创建，已存在就 `show()/hide()` 切换。
  新增 IPC：`ipcMain.on('timers-toggle', …)`。
- **preload.js**：`window.widget` 加 `toggleTimers()`；通知用渲染进程
  `new Notification()` 即可（无需新 IPC）。
- **入口按钮**：`clock.html/clock.js` 番茄钟视图加小按钮。有计时器在走时
  显示状态点，tooltip 显示「学习 · 剩 35:12」。

### 2.1 关窗保护（已定）

点窗口关闭按钮时，若有计时器在走，**拦截关闭并弹确认框**（窗口内 dialog 或
主进程 `dialog.showMessageBox`）：

> 「学习」还在计时中
> [ 继续计时并关闭 ]  [ 停下并结算 ]

- 继续计时：窗口关闭，`running + lastStart` 保留，下次打开按真实时间差结算。
- 停下：先 `usedMs += 差值`、`running = false`，再关窗。
- 没有在走的计时器 → 直接关，不打扰。

## 3. 核心概念与数据模型

### 3.1 两种计时语义（一个模型，两种展示）

| 类型 | 例子 | 语义 | 视觉 |
|---|---|---|---|
| 限额型 `limit`（玩） | 玩游戏 1h | 预算**不能超过**；显示**剩余**时间 | 剩余 ≤ 5min 变黄；归零变红继续计超支（`+12:30`），**归零瞬间弹轻提醒** |
| 目标型 `goal`（学习/工作） | 写作业 1h | 预算**计划要完成**；显示**已用/目标** | 进度条走满变绿，**达标瞬间弹轻提醒**，超标不变红 |

统一存 `budgetMin` + `usedMs`，展示层按 `mode` 解释，不做两套数据。

### 3.2 存储（`localStorage`，key = `timer.v1`）

`timer.html` 与 `clock.html` 共享同一 session 的 localStorage，时钟入口按钮
直接读同一份数据做状态点，无需额外 IPC。

```js
{
  version: 1,
  timers: [
    {
      id: 1730000000000,        // Date.now()
      name: '玩游戏',
      mode: 'limit',            // 'limit' | 'goal'
      budgetMin: 60,
      cycle: 'day',             // 'day' | 'week' | 'manual'
      usedMs: 0,
      running: false,
      lastStart: null,
      cycleKey: '2026-08-31',   // 本周期标识，用于跨天判断
      todoId: null,             // 关联的提醒事项 id（可空，见 §5.2）
      notified: false           // 本周期是否已发过提醒，避免重复弹
    }
  ]
}
```

### 3.3 跨天（周期清零）逻辑

- 窗口加载时 + 每 30s 心跳重算当前 `cycleKey`：`day` → `YYYY-MM-DD`；
  `week` → ISO 周；`manual` → 永不清零。
- `cycleKey` 不匹配 → **静默清零**（`usedMs=0, running=false, notified=false`）。
- 清零不弹通知、不总结、不打分——纯自律。
- 时钟入口按钮读数据前先跑同样的周期比对，避免短暂显示旧值。

### 3.4 计时精度（关键实现约束）

- **绝不用 `setInterval` 累加**——Electron 渲染进程后台会被节流。
- 开始记 `lastStart = Date.now()`；暂停 `usedMs += 差值`；界面每秒按
  `Date.now()` 差值重算显示；存储在 开始/暂停/心跳/卸载 时落盘。
- 关窗期间计时继续走（数据模型与窗口开关无关），重开按真实时间差结算。

### 3.5 单跑道规则

同一时刻只允许一个计时器在走：点开 A 时 B 自动暂停。

## 4. UI 规划

### 4.1 入口（时钟番茄钟视图内）

番茄钟界面角落一个小秒表图标按钮，不抢主视觉；有计时器在走时带状态点。

### 4.2 计时器窗口（白底，macOS 应用风）

```
┌─────────────────────────────┐
│  今天的计时器          ⟳  ＋ │   ← 全部重置 / 新建
├─────────────────────────────┤
│  ● 玩游戏          剩 42:13 │   ← 限额型：左名称，右剩余
│  ▓▓▓▓▓▓░░░░░░       ⏸      │
│  ○ 学习            25:10/1h │   ← 目标型：已用/目标
│  ▓▓▓░░░░░░░░░       ▶      │
├─────────────────────────────┤
│  玩 · 已用 18/60min          │   ← 底部合计：按类型分两行
│  学习 · 已达 1h07m/3h        │
└─────────────────────────────┘
```

- **行交互**：点行播放/暂停；双击改名；行尾 ⋯ 菜单：
  「编辑时长 / 切换类型 / 改周期 / 重置本条 / 删除」（删除无挽留，但二次确认防误点）。
- **新建流程**：点 ＋ 底部展开内联表单（不弹窗）：
  - 第一行：名称输入框，**右边一个「从提醒事项选」按钮**（见 §5.2）；
  - 第二行：快捷时长 15m / 30m / 1h / 2h + 自定义；
  - 第三行：类型（限额·玩 / 目标·学习）+ 周期（**默认按天**，下方可按周/手动）。
- **状态色**：灰=未开始，蓝=进行中，绿=达标，红=超支；颜色全部走 DESIGN.md 变量。
- **空态**：一行灰字「点 ＋ 给第一件事设个时间额度」。
- 数字一律 `tabular-nums`；列表行有 45ms 级联入场动画（沿用 todo.js 的手法）。

### 4.3 提醒（已定：要有）

- 限额型归零瞬间、目标型达标瞬间：一条系统通知（`new Notification()`）+
  沿用 clock.js 的轻柔双音提示音；每条计时器每周期只提醒一次（`notified` 位）。
- 通知文案极简：「玩游戏 额度用完了」/「学习 目标达成 ✓」。无按钮、无后续动作。

## 5. 与其他组件的联动

### 5.1 番茄钟：「用来：其他 ›」（已定，本期做）

番茄钟**拨时长界面底部多一栏**：`用来：其他 ›`，点击展开一个选择列表：

- `其他`（默认）——纯番茄钟，不计入任何计时器；
- 计时器列表里的每一项——选中后显示 `用来：学习 ›`。

**同步语义**：番茄钟**自然走完**时（与 `recordSession` 同一时机）把整段时长计入；
**中途清空 / 半路退出也会把已过的时间计入**（跑了 30 分钟清空，计时器就多 30 分钟，
不白干）。番茄钟开跑即全系统单通道：所有在走的计时器自动暂停结算。
番茄钟运行期间，该计时器行显示「🍅 专注中」标记但不重复走字。选择记忆在 `pomo.link` key 里，
下次开番茄钟沿用上次选择。

### 5.2 新建计时器：「从提醒事项选」（已定，本期做）

新建表单名称栏右侧的按钮：点击弹出当前**未完成待办**列表
（复用现有 `window.widget.getSessionCandidates()`，main.js:296 已聚合 todo 数据），
选中即填入名称并把 `todoId` 存进计时器。纯关联命名，本期不做完成状态双向同步
（v2 可选：计时器达标后问一句要不要勾掉对应待办）。

### 5.3 日历（v2 可选）

`pushDaylog('timer', …)` 广播每日各计时器用量，日历格子多一个回顾维度。先留接口。

## 6. 平台接入改动清单

| 文件 | 改动 |
|---|---|
| `timer.html` / `timer.js` | 新建。白底 macOS 应用风窗口；引 `settings.js`（字体），不引 `hitcorner.js` |
| `main.js` | 仿 `managerWin` 加 `timerWin` 单例 + `timers-toggle` IPC；关窗保护若用主进程 dialog 也在这 |
| `preload.js` | 加 `toggleTimers()`（以及关窗确认所需的一两条 IPC） |
| `clock.html` / `clock.js` | 番茄钟视图：入口小按钮 + 状态点；拨时长界面加「用来：其他 ›」栏；`recordSession` 同步写 `timer.v1` |
| `widgets.js` | **不动**——不是注册小组件，不占 REGISTRY |

## 7. 自检

- `timer.html` 支持 `?demo` 注入三条演示数据（沿用 todo.js 的 DEMO 模式）供截图验收。
- `--selftest` 覆盖时钟新增的入口按钮与「用来」栏（可用 `POMO_PICK` 类环境变量控演示态）。
- 手动验证清单：关窗保护两分支、跨天清零、单跑道互斥、番茄钟同步计入、
  提醒只发一次、深浅主题各过一遍。

## 8. 开发步骤（建议顺序）

1. `main.js` + `preload.js`：`timerWin` 单例、`timers-toggle`、关窗确认。
2. `timer.html` 骨架 + `timer.js` 数据模型（含跨天清零、关窗结算）。
3. 行渲染 + 播放/暂停 + 单跑道互斥。
4. 新建/编辑/删除/重置 +「从提醒事项选」。
5. 提醒（通知 + 提示音 + notified 位）。
6. 时钟侧：入口按钮 +「用来：其他 ›」+ `recordSession` 同步。
7. macOS 风样式打磨（按 DESIGN.md 核对清单逐项过）。
8. （v2）日历 daylog、待办完成双向同步。
