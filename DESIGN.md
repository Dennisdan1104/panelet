# panelet 设计语言

任何新组件/新界面都应遵守本文。规则背后的"为什么"比规则本身重要——
尤其几何与阴影那几条，是这个项目被用户骂出来的教训。

## 1. 设计令牌

> 颜色收敛为**五种固定风格**（solid 凝实 / frost 通透 / dark 深色紫 /
> cream 暖沙 / ink 墨玉），自由调色板已移除。切换走控制中心或 settings.style。
> （styles.css `:root` + settings.js 主题表）

所有颜色一律走 CSS 变量，**禁止在新代码里写死色值**。
用户在控制中心改主题/强调色/文字色时，settings.js 会覆写这些变量。

| 变量 | 语义 | 深色默认 | 浅色默认 |
|---|---|---|---|
| `--bg-hi` / `--bg-lo` | 卡片渐变背景（左上→右下） | rgba(48,48,55,.94) / rgba(22,22,27,.92) | 近白奶白（更通透） |
| `--text` | 主文字 | #f7f7fa | #1d1d21 |
| `--dim` / `--dimmer` | 次级/三级文字 | 同色 62%/36% 透明 | 同左 |
| `--accent` | 强调色，随三种固定风格内置 | solid/frost: #0a84ff | dark: #bf5af2 |
| `--wash` / `--track` / `--hairline` / `--stroke` | 分层底色/轨道/发丝线/描边 | 白系低透明 | 黑系低透明 |
| `--orange` | 提醒事项身份色、上午/下午 | #ff9f0a | |
| `--red` | 周末、删除、危险动作 | #ff453a | |
| `--radius` | 卡片圆角 | 26px | |

主题双轨：深色玻璃用**黑色内晕**做暗角；浅色是"奶白磨砂"用**白色内晕**，
绝不把深色那套黑阴影带进浅色（会变灰泥边）。

## 2. 几何与阴影 —— 最重要的一节

1. **卡片窗口尺寸 ≡ 可见卡片尺寸**。绝不为阴影/光晕留透明边距。
   历史教训：44px 阴影边距造成一圈"点不到桌面"的死区。
2. **卡片禁止外投影（outer box-shadow）**。立体感只用内阴影：
   `inset 高光 + inset 底部暗线 + inset 大范围暗角`。
3. 圆角外的四个小像素区域由 `hitcorner.js` 动态穿透
   （半径 = 26 × 当前缩放，勿改卡片圆角时忘了同步它）。
4. 控制中心是**例外**：不透明窗口 + 系统 DWM 原生圆角阴影。
   原因：Windows 透明窗口有"间歇性白框"缺陷，大面板赌不起。
   新的大面板类窗口一律照此办理；小卡片才允许透明窗口。

## 3. 动效

| 场景 | 参数 |
|---|---|
| 卡片入场 | .55s cubic-bezier(.2, 1.25, .35, 1)（带回弹） |
| 列表行进入 | .3s，逐行 45ms 递增，最多 350ms |
| 抽屉/滚轮展开 | max-height .32s cubic-bezier(.3, .9, .35, 1)（无回弹，干脆） |
| 勾选弹性 | .18s–.22s cubic-bezier(.3, 1.4+, .5, 1)（超调感） |
| 窗口伸缩补间 | 主进程 ease-out cubic，步长按像素距离 2–10 步 |

原则：**位移动画干脆，状态动画弹性**；时长不超过 .55s。

## 4. 字体与数字

- 字体栈：`"SF Pro Display", "Segoe UI Variable Display", "Segoe UI", system-ui`
  （不打包任何苹果字体，靠系统回退）
- 所有计时/计数文本必须 `font-variant-numeric: tabular-nums`，否则跳动
- 大数字用 font-weight 200–300 的细体 + 垂直渐变（`--dig1 → --dig2`）

## 5. 交互范式（复用而非重造）

- **勾选**：19px 圆圈，空心 → 填充强调色 + 白勾弹入（见 todo 的 `.chk`）
- **分段控件**：胶囊容器 + 选中项强调色胶囊反白（`.modeseg`）
- **数字滚轮**：4 列 26px 行高，中央选中带 + 上下 `mask-image` 渐隐，
  滚轮事件与点击都可选；记得容器要 `-webkit-app-region: no-drag`
- **抽屉**：点箭头展开为卡片附加高度块，块内无框、纯行排版
- **主按钮**：强调色填充、圆角 13px、发光 `color-mix(accent 40%)`，
  禁用态 = 38% 透明无影；次要按钮 = 幽灵样式（细边框 + hover 洗底色）

## 6. 纵向伸缩协议（架构级约定）

组件需要向下展开内容时：

1. 渲染端维护**唯一权威**：`currentExtra()` 纯函数由状态推导所需附加高度；
2. 通过 `window.widget.resizeCard(绝对值)` 声明总需求——**永远不发增量**；
3. 主进程 `baseHTarget(缩放基准) + extraAcc(声明值)` 得出目标并补间；
4. 渲染端每秒对账自愈（`setInterval` 核对声明值与已发送值）。

配套守护：`electron . --selftest --interact` 会对完整开合序列做高度断言。
**任何改动伸缩相关代码后必须跑它**。历史教训：增量累加 + 两本账键不一致，
导致过"只涨不跌"的窗口，修了四轮才定位。

## 7. 新组件核对清单

- [ ] 注册进 `widgets.js` 的 REGISTRY（见 WIDGETS.md）
- [ ] 根元素 `.card`，页面引入 settings.js + hitcorner.js
- [ ] 颜色全部用变量；深浅两套主题下都检查一遍
- [ ] 数字用 tabular-nums
- [ ] 右键挂 `window.widget.openMenu('<id>')`
- [ ] 需要 localStorage 时用独立 key（`<id>.v1`）
- [ ] 自检：`--selftest` 截图肉眼过 + 若有伸缩则跑 `--interact`
